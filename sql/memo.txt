https://devhints.io/mysql
https://wiki.archlinux.org/title/JDBC_and_MySQL
mysql -u daar -p daar;
password

SETUP SQL
sudo mysql -u root -p < reset_db.sql;
sudo mysql -u root -p < configure_db.sql;


SHOW DATABASES;
SHOW TABLES;
SELECT * FROM Livre;
